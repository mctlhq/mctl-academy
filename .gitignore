node_modules
dist/
.env
.env.*
!.env.example
*.log
.DS_Store
coverage/


# Playwright (e2e.yml) output — reports/traces from local or CI runs.
playwright-report/
test-results/

# build-mock-bundle.mjs writes this at build time (docker/CI pre-hook).
client/src/data/mock-bundle.generated.json

# Scratch space for the Content replenish workflow (candidates, captured
# snapshots, PR body). Keeping the workflow's own intermediate files here is
# what lets its boundary check be "nothing untracked outside
# content/questions" rather than a list of filenames to keep in sync.
#
# What the AGENTS write is not here: it lives in _agent/, which is
# deliberately NOT ignored so that a stray file there is visible to a human
# reading `git status` after a failed run. boundaryProblems excludes both
# directories by pathspec.
_run/
